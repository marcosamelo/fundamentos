const raio = 5.6;
const area = Math.PI * Math.pow(raio, 2);// Math e um Objeto que possui uma função constante do PI e a variavel Pow que e uma pontencia 

console.log(area);
console.log(typeof Math)
